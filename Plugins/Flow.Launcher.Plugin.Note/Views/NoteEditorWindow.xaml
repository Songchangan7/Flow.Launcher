<Window
    x:Class="Flow.Launcher.Plugin.Note.Views.NoteEditorWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="{DynamicResource flowlauncher_plugin_note_editor_window_title}"
    Width="680"
    Height="520"
    MinWidth="560"
    MinHeight="420"
    Background="{DynamicResource PopupBGColor}"
    Foreground="{DynamicResource PopupTextColor}"
    ResizeMode="CanResize"
    WindowStartupLocation="CenterScreen"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
            <RowDefinition Height="80" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel Margin="26 18 26 12">
                <TextBlock
                    x:Name="EditorTitle"
                    FontSize="22"
                    FontWeight="SemiBold"
                    TextAlignment="Left" />
                <TextBlock
                    x:Name="EditorSubtitle"
                    Margin="0 10 0 0"
                    FontSize="14"
                    TextWrapping="Wrap" />
            </StackPanel>

            <Button
                Grid.Column="1"
                Click="CancelEdit"
                Style="{StaticResource TitleBarCloseButtonStyle}">
                <Path
                    Width="46"
                    Height="32"
                    Data="M 18,11 27,20 M 18,20 27,11"
                    Stroke="{Binding Path=Foreground, RelativeSource={RelativeSource AncestorType={x:Type Button}}}"
                    StrokeThickness="1" />
            </Button>
        </Grid>

        <Grid Grid.Row="1" Margin="26 0 26 18">
            <TextBox
                x:Name="EditorTextBox"
                AcceptsReturn="True"
                AcceptsTab="True"
                HorizontalScrollBarVisibility="Disabled"
                TextWrapping="Wrap"
                VerticalScrollBarVisibility="Auto" />
        </Grid>

        <Border
            Grid.Row="2"
            Background="{DynamicResource PopupButtonAreaBGColor}"
            BorderBrush="{DynamicResource PopupButtonAreaBorderColor}"
            BorderThickness="0 1 0 0">
            <StackPanel HorizontalAlignment="Center" Orientation="Horizontal">
                <Button
                    x:Name="CancelButton"
                    MinWidth="150"
                    Margin="0 0 6 0"
                    Click="CancelEdit" />
                <Button
                    x:Name="ConfirmButton"
                    MinWidth="150"
                    Margin="6 0 0 0"
                    Click="ConfirmEdit"
                    Style="{DynamicResource AccentButtonStyle}" />
            </StackPanel>
        </Border>
    </Grid>
</Window>
